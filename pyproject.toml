[project]
name = "ah-connect"
version = "1.0.0"
description = "Home Assistant custom integration for Albert Heijn inspired by appie-go"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
